name = "edge-ai-inference"
main = "src/worker.ts"
compatibility_date = "2024-12-30"

# Edge Runtime 设置
# 注意：AI 推理需要合理的 CPU 时间

[vars]
# 模型仓库基础 URL（例如 HuggingFace 或自有 CDN）
MODEL_BASE_URL = "https://cdn.example.com/models"

# 可选：绑定 Workers AI（如果使用 Cloudflare 托管模型）
# [[ai]]
# binding = "AI"
