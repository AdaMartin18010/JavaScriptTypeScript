# 短路求值与逻辑赋值

> 逻辑运算符的求值策略与复合赋值语法

---

## 内容大纲（TODO）

### 1. 短路求值基础

- `&&`：左假则停
- `||`：左真则停
- `??`：左非 nullish 则停

### 2. 逻辑运算符的应用模式

- 默认值模式（`||` / `??`）
- 条件执行模式（`&&`）
- 链式默认值

### 3. 逻辑赋值运算符（ES2021）

- `&&=`：左真则赋值
- `||=`：左假则赋值
- `??=`：左 nullish 则赋值

### 4. 与类型系统的交互

- 类型收窄效果
- 控制流分析中的逻辑运算符

### 5. 常见陷阱

- `||` 与 `??` 的语义差异
- 0 / "" / false 的默认值处理
- 逻辑运算符的优先级
