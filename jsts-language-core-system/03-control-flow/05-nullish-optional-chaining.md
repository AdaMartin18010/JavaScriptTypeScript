# 空值合并与可选链

> ?? 与 ?. 的组合模式与最佳实践

---

## 内容大纲（TODO）

### 1. 可选链操作符 `?.`

- 属性访问可选链
- 计算属性可选链
- 函数调用可选链
- 短路行为

### 2. 空值合并运算符 `??`

- nullish 的定义（null / undefined）
- 与 `||` 的区别
- 与可选链的组合

### 3. 组合模式

- `obj?.prop ?? default`
- 深层属性安全访问
- 数组元素安全访问

### 4. 类型系统支持

- TS 3.7+ 的可选链类型推断
- 可选链后的类型收窄

### 5. 常见陷阱

- 可选链不能赋值左侧
- `?.` 与 `!` 非空断言的混用
- 过度使用可选链的可读性问题
