# 顶层 await

> ES2022 顶层 await 的执行语义与模块加载顺序影响

---

## 内容大纲（TODO）

### 1. 语法与基本语义

- 模块顶层使用 await
- 隐式 async 模块包装

### 2. 模块加载顺序影响

- 顶层 await 导致的模块执行暂停
- 依赖模块的加载阻塞
- 循环依赖的处理

### 3. 与动态导入的结合

- `await import()`
- 条件导入

### 4. 浏览器与 Node.js 支持

- 模块类型要求
- 错误处理

### 5. 最佳实践

- 避免过长的顶层 await
- 并行加载策略
- 降级方案

### 6. 常见陷阱

- 顶层 await 导致的启动延迟
- 隐式模块 async 的副作用
